#include <stdio.h>
#include <stdlib.h>

//using namespace std;

#define maxNum(a, b) ((a > b) ? a : b)

int main () {
	int num_1;
	int num_2;
	int diff;
	int diffABS;

	printf("Enter 1st integer:\t");
	scanf("%d", &num_1);

	printf("Enter 2nd integer:\t");
	scanf("%d", &num_2);

	printf("hi long there %dhow's ? the \"\\\""/*double)""\""*/, (int) 6
);
	printf(/* for %f if */"hi \"there\\"//while*/")
	, (float) 2);

    printf("hi %ld there\"\\"/*float" char short */, (long) 8);

	double a;
	printf("for");
	short s;
	printf(" for ");
	int q;
	printf("for ");
	long p;
	printf(" for");
	double c;
	// for
	short m;
	/*
		for
	*/
	int j;

	while (sizeof(int));

	printf ( "\" malloc for-if else /* continue %f break \\\"", (float) 1);
	printf ( " malloc for-if else continue %ld break \"", (long)2 );
	printf ("malloc for-if // else continue %hd break " ); (short) (1); {}
	printf (" malloc for - if else continue */ break ");
	printf ( "malloc for-if \" else %d %hdcontinue break", (int) 5,*(short*)&m);
	printf ( "malloc for-if \" else\" continue break");
	printf ( "malloc for-if \"else\"\" continue break");

	diff = num_1 - num_2;
	/*for*/
	/*while */
	/* while*/
	/* if */

	/**/
	/*intwhile*/// wherex while for if


	/*hello
	int while for
	*/short as;

	/*hello
	int while for
	*//**/char b;

	/*"for*/
	/*while" *//*pncho*/
	/* "while"*/int ci=(int)7;

	/**/// int while for /* double */
	// /* switch */

	/* if */

	diffABS = abs(diff);

	switch (diffABS) {
		case /* switch for if else */ 0:	printf("\nNumbers are equal\n");
			break;

		default: if (diff > 0) {
				printf("\n%d is greater than %d\n", num_1, num_2);
			} else {
				// if
				// for
				// while
				printf("\n%d is greater than %d\n", num_2, num_1);
			}
	}

	return 0;
}

